[project]
name = "fleetiq-observability"
version = "0.1.0"
description = "Structured logging and secret redaction for FleetIQ services."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fleetiq_observability"]
