# Dataset Tools

Dataset utilities generate reproducible analysis notebooks without changing
the organizer dataset layout. Run `generate_notebooks.py` from the repository
root; generated notebooks live in `ml/notebooks/`.
