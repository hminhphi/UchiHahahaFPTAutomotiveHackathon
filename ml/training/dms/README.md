# DMS Training

Offline-only workspace for driver-state dataset preparation, training, and
evaluation. Runtime services must not import this package. Store configurations
under `ml/configs`, checkpoints under ignored `artifacts/models/checkpoints`,
and reproducible metrics under ignored `artifacts/training`.
