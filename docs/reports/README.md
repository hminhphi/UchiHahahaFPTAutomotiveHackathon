# Reports

Purpose: Stores committed progress, evaluation, and release reports.

Owner: Product Lead and workstream owners.

- `progress/`: milestone one-pagers and review-ready progress exports.
- Future model evaluation reports should state dataset version, model version,
  metrics, owner, and reproducible command.

Large generated media and raw predictions belong under `artifacts/reports/` or
`artifacts/renders/`.
